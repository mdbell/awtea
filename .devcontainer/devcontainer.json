{
  "name": "AWTea Development",
  "image": "mcr.microsoft.com/devcontainers/java:1-11-bullseye",
  
  "features": {
    "ghcr.io/devcontainers/features/docker-in-docker:2": {
      "version": "20",
      "moby": true
    },
    "ghcr.io/devcontainers-contrib/features/deno:1": {
      "version": "v2.x"
    }
  },

  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vscjava.vscode-gradle",
        "redhat.java",
        "vscjava.vscode-java-debug",
        "vscjava.vscode-java-test",
        "denoland.vscode-deno",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "deno.enable": false,
        "deno.enablePaths": [
          "./awtea-graphics/src/test/deno"
        ]
      }
    }
  },

  "forwardPorts": [8080, 3000],

  "postCreateCommand": "java -version && ./gradlew -v && deno --version && docker --version",

  "remoteUser": "vscode",
  
  // Mount Docker socket to enable Docker-in-Docker for WASM compilation
  // Note: This grants container access to the host's Docker daemon
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ]
}
