{
  "name": "AWTea Development",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  
  "customizations": {
    "vscode": {
      "extensions": [
        "vscjava.vscode-java-pack",
        "vscjava.vscode-gradle",
        "redhat.java",
        "vscjava.vscode-java-debug",
        "vscjava.vscode-java-test",
        "denoland.vscode-deno",
        "ms-azuretools.vscode-docker"
      ],
      "settings": {
        "deno.enable": false,
        "deno.enablePaths": [
          "./awtea-graphics/src/test/deno"
        ]
      }
    }
  },

  "forwardPorts": [8080, 3000],

  "postCreateCommand": "source /home/vscode/.sdkman/bin/sdkman-init.sh && source /opt/emsdk/emsdk_env.sh && java -version && ./gradlew -v && deno --version && emcc --version",

  "remoteUser": "vscode",
  
  // Mount Docker socket to enable running Docker commands for WASM compilation
  // Note: This grants container access to the host's Docker daemon
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ]
}
